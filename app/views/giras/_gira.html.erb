<div class="col-md-3 col-3 mb-3">
  <div class="card">
    <h5 class="card-header"><%= gira.get_day %></h5>
    <div class="card-body">
      <h5 class="card-title"><%= gira.name %></h5>
      <p class="card-text"><%= gira.type_of_gira %></p>
      <% if user_signed_in? %>
        <a href="#" class="btn btn-primary btn-sm">Marcar Presença</a>
      <% end %>
    </div>
  </div>
</div>